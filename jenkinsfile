pipeline {

    agent any

    triggers {
        cron('0 9 * * *')
    }

    parameters {
        choice(
            name: 'PUBLISH_MODE',
            choices: ['auto', 'preview'],
            description: 'auto = publish directly | preview = pause for manual approval'
        )
        string(
            name: 'BLOG_TOPIC',
            defaultValue: 'Python automation tips for beginners',
            description: 'Topic for the AI-generated blog post'
        )
    }

    environment {
        DEVTO_EMAIL    = credentials('DEVTO_EMAIL')
        DEVTO_PASSWORD = credentials('DEVTO_PASSWORD')
        GEMINI_API_KEY = credentials('GEMINI_API_KEY')
        BLOG_TOPIC     = "${params.BLOG_TOPIC}"
    }

    stages {

        stage('Checkout') {
            steps {
                echo '--- Checking out source code ---'
                checkout scm
            }
        }

        stage('Install Dependencies') {
            steps {
                echo '--- Installing Python dependencies ---'
                bat '"C:\\Users\\Yash Naik\\AppData\\Local\\Programs\\Python\\Python314\\Scripts\\pip.exe" install selenium requests --quiet'
            }
        }

        stage('Generate & Publish Blog') {
            steps {
                echo "--- Running blog pipeline ---"
                bat "\"C:\\Users\\Yash Naik\\AppData\\Local\\Programs\\Python\\Python314\\python.exe\" selenium/devto_poster.py --mode ${params.PUBLISH_MODE} --headless"
            }
        }

    }

    post {
        success {
            echo '✅ Blog post published successfully!'
        }
        failure {
            echo '❌ Pipeline failed. Check the logs above.'
        }
    }
}
